# Core data & ML (pinned per project_gagan -- verified working versions)
numpy==2.0.2
pandas==2.3.3
scikit-learn==1.6.1
scipy==1.13.1
joblib==1.5.3

# Data I/O (reading ipl_data.xlsx)
openpyxl==3.1.5

# Interpretability (src/explainability.py)
shap==0.49.1

# Alternative Transformer model, MC-Dropout engine
# (src/transformer_model.py, alt_transformer_*.py, win_probability_engine.py)
# DEF-012: pinned exactly (was torch>=2.1, which permitted dependency drift).
torch==2.8.0

# Testing
pytest==8.4.2

# ── Notes ───────────────────────────────────────────────────────────────────
# XGBoost: all tables labelled 'XGBoost' use sklearn HistGradientBoosting as
# a drop-in equivalent (project_gagan's pattern, kept as the default to avoid
# a hard Homebrew/libomp dependency -- see docs/known_limitations.md).
# To use native XGBoost instead:
#   brew install libomp && pip install xgboost>=2.0
#
# Not ported from either source project:
#   - tensorflow/keras (project_gagan's LSTM) -- not ported; the calibrated
#     Logistic Regression dynamic model already outperforms the LSTM at
#     every reported horizon (see DEEP_COMPARISON.md Table 5), so this
#     avoids a heavy TensorFlow dependency for no accuracy gain.
#   - torch_geometric (project_hrishav's GraphSAGE GNN) -- not ported; see
#     src/transformer_model.py's PlayerEmbedTable docstring.
#   - optuna/lightgbm/catboost (project_hrishav's tuned baselines) -- not
#     ported; project_gagan's calibrated LogReg/GBT zoo is the default
#     win-probability path per the approved merge decisions.
